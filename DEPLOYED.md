# Chatbot v2 deployed

The improved embedded-JSON chatbot is deployed on `main` as `index.html`.

- Korean natural-language query normalization
- amount expression normalization
- synonym expansion
- sentence-level retrieval
- answer synthesis with source sections
- embedded JSON, no external JSON file required
